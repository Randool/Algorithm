#include <bits/stdc++.h>
using namespace std;

class Solution {
private:
    int len;
    vector<string> result;
    void helper(int cur) {
        if ()
    }
public:
    vector<string> letterCasePermutation(string S) {
        len = S.length();
        for (int i = 0; i < len; ++i) {
            if ()
        }
    }
};

int main() {

	return 0;
}

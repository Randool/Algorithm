#include <cstdio>
using namespace std;

#define MAXN 505
#define show(x) printf("%d", x)
#define shown(x) printf("%d\n", x)

int main() {
    FILE *fi = fopen("input.txt", "r");
    // FILE *fo = fopen("output.txt", "w");

    int n;

    fscanf(fi, "%d", &n);

    return 0;
}
